package jack.javacore.classic.examples.DateTime;

/*import java.time.Instant;
import java.time.LocalDateTime;
import java.time.ZoneId;
import java.util.Calendar;
import java.util.Date;*/

public class LegacyDates {
/*
    public static void main(String[] args) {
        new LegacyDates().parseLegacyDate(new Date(), Calendar.getInstance());
    }

    public LocalDateTime parseLegacyDate(Date date, Calendar calendar) {
        System.out.println(new Date().toString());

        LocalDateTime dateTime = LocalDateTime.ofInstant(new Date().toInstant(), ZoneId.systemDefault());
        System.out.println(dateTime);

        LocalDateTime dateTime1 = LocalDateTime.ofInstant(Calendar.getInstance().toInstant(), ZoneId.systemDefault());
        System.out.println(dateTime1);

        return null;
    }*/
}
